# 81ee19e2 — DPI Aware updates.

- **Date:** 2020-12-12  |  **Risk (A.1):** HIGH (filename proxies)
- **Files:** 9 (+648/−599) — dominated by a 1022-line `PadControl.Designer.cs` relayout

## Changes

### Fixes
- Re-lays-out the whole `PadControl` designer so the pad-mapping page fits when
  the process is DPI-aware (the fix for clipped labels with crisp rendering).
- Flips the `IsProcessDPIAware` backing field default to `true` (attribute
  stays `false` until [9832e66e](9832e66e.md)).

### Updates
- `_TempUserControl` → `_TempWIN2WPF.com` scaffolding renames + csproj — WPF
  rewrite housekeeping, not wanted.

## Backward compatibility for 4.17.x
- No contract impact, but the designer diff sits on top of the Macros commits
  (f4cd78c7, a18da263) which we skip — the pre-image does not match our tree,
  so it cannot be snapshot-ported.

## Decision
**Postpone.** The [347427e9](347427e9.md) port (DPI-aware opt-in, default off)
already fixes rendering via bitmap scaling. Crisp-DPI + relayout is a later,
larger step: port the PadControl relayout manually once the test harness (C)
exists, together with [9832e66e](9832e66e.md).

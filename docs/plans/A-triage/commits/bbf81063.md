# bbf81063 — Fix entity query.

- **Date:** 2021-08-09  |  **Risk (A.1):** HIGH (web_services bucket)
- **Files:** 1 (+9/−9) — `x360ce.Web/WebServices/x360ce.asmx.v4.cs`

## Changes

### Fixes
- `GetVendors()` projected onto the mapped entity type *inside* a LINQ-to-Entities
  query, which Entity Framework rejects at runtime ("The entity or complex type
  cannot be constructed in a LINQ to Entities query"). Materializes with
  `.ToList()` first, then projects in memory.

## Backward compatibility for 4.17.x
- Implementation-only; the SOAP contract (method signature, `Vendor` shape) is
  unchanged. Pre-image matches the restored tree exactly.

## Decision
**Apply.** APPLIED on branch. Builds under `APP_Any_v4`.

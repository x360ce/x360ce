# 84550a9b — Remove unused packages. Fix test device crash.

- **Date:** 2024-08-08  |  **Risk (A.1):** MEDIUM
- **Files:** 2 — `x360ce.App.Beta/Common/TestDeviceHelper.cs` (+1/−1), Engine csproj (package removal)

## Changes

### Fixes
- Virtual test controller POV sweep: the branch selector compared the wrong
  variable (`if (time <= half)`) instead of the shifted position
  (`if (value < half)`), feeding out-of-range values into
  `ConvertHelper.ConvertRange` (the titled crash).

### Updates
- Package removal in Engine csproj — 2024-era project state, not ported.

## Backward compatibility for 4.17.x
- Pre-image matches the restored tree exactly (same code, same bug).

## Decision
**Apply** (code hunk only). APPLIED on branch at `App.v4/Common/TestDeviceHelper.cs`.

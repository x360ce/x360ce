# f295e72e — Updates.

- **Date:** 2020-11-15  |  **Risk (A.1):** HIGH (filename proxy `OptionsUserControl.Designer.cs` — false positive, UI layout only)
- **Files:** 2 (+24/−22) — `x360ce.App.Beta/Common/DInput/DInputHelper.Step5.VirtualDevices.cs`, `x360ce.App.Beta/Controls/OptionsUserControl.Designer.cs`

## Changes

### Fixes
- `GetGuideKeys()` (added by [2c6a14fe](2c6a14fe.md)): `Enum.TryParse(s, out keyValue)`
  → `Enum.TryParse(s, true, out keyValue)` — key names in `GuideButtonAction` now
  parse case-insensitively (`{lwin}` works, not just `{LWin}`). Plus two explanatory
  comments.

### Updates
- `OptionsUserControl.Designer.cs`: pure layout shuffle (ViGEm group box resized/
  repositioned, control coordinates moved). Cosmetic; one moved label gets a negative
  X coordinate — churn, not a fix.

### Additions / Removals
- None.

## Backward compatibility for 4.17.x
- Step5 hunk: unaffected contracts; only meaningful on top of 2c6a14fe.
- Designer hunk: cosmetic UI churn from the rewrite era; not wanted.

## Decision
**Partial** (final). The Step5 hunk is APPLIED on branch, folded into the
[2c6a14fe](2c6a14fe.md) port. The Designer layout hunk is not ported — it was
briefly included as the pre-image base for [347427e9](347427e9.md), but that
port was reverted (see its analysis), so `OptionsUserControl.Designer.cs`
stays at the 4.17.12 state.

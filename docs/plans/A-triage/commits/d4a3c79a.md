# d4a3c79a — v4.17.15.0 - Fixed: Crash when selecting unavailable game. - Fixed: Crash when loading user device capabilities.

- **Date:** 2020-11-15  |  **Risk (A.1):** LOW
- **Files:** 3 (+18/−9) — `x360ce.App.Beta/Common/DInput/DInputHelper.Step1.UpdateDevices.cs`, ChangeLog, AssemblyInfo

## Changes

### Fixes
- `RefreshDevice(...)` in `DInputHelper.Step1.UpdateDevices.cs`: wraps two blocks in
  `lock (SettingsManager.UserDevices.SyncRoot)` —
  1. the `new Joystick(...)` / `ud.LoadCapabilities(...)` try-block,
  2. the `ud.LoadInstance(device)` call.
  Prevents "Collection was modified; enumeration operation may not execute" crash
  while loading user device capabilities. (Subject repeats the v4.17.14 line; the
  new content is only the lock fix.)

### Updates
- AssemblyInfo version bump 4.17.13.0 → 4.17.15.0 (not ported — versioning is
  handled separately on this branch).

### Additions / Removals
- None.

## Backward compatibility for 4.17.x
- **Settings.xml / WebService API / data model:** unaffected. Thread-safety only,
  and it reuses the same `SyncRoot` locking pattern that already exists elsewhere in
  the same method (current lines 161, 174).
- Restored tree verified: `App.v4/Common/DInput/DInputHelper.Step1.UpdateDevices.cs:144-154`
  — both fix spots are still unlocked.

## Decision
**Apply** (code hunk only). Path translation: `x360ce.App.Beta/` → `App.v4/`.
Skip ChangeLog and AssemblyInfo hunks.

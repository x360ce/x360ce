# 1088022a — Fixes

- **Date:** 2020-11-15 (last commit of the day, after v4.17.15.0)  |  **Risk (A.1):** LOW
- **Files:** 2 (+6/−3) — `x360ce.App.Beta/Common/DInput/DInputHelper.Step1.UpdateDevices.cs`, `x360ce.App.Beta/Common/TestDeviceHelper.cs`

## Changes

### Fixes
- Continuation of the [d4a3c79a](d4a3c79a.md) thread-safety fix: wraps three
  `IsOnline` writes in `lock (SettingsManager.UserDevices.SyncRoot)` —
  1. `deleteDevices[i].IsOnline = false` (device removal loop),
  2. `ud.IsOnline = true` (refresh path),
  3. `item.IsOnline = true` in `TestDeviceHelper.EnableTestInstances()` (the
     virtual test controller).

### Updates / Additions / Removals
- None.

## Backward compatibility for 4.17.x
- Unaffected contracts; same locking pattern as its siblings.

## Decision
**Apply.** APPLIED on branch via file snapshot at this commit (carries d4a3c79a
too). Path translation: `x360ce.App.Beta/` → `App.v4/`.

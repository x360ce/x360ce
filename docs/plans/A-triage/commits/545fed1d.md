# 545fed1d — v4.17.14.0 - Fixed: Crash when selecting unavailable game.

- **Date:** 2020-11-15  |  **Risk (A.1):** LOW
- **Files:** 2 (+4/−1) — `x360ce.App.Beta/Global.cs`, ChangeLog

## Changes

### Fixes
- `Global.cs` (game-selection handler): `LastActivePath = game.FullPath;` →
  `LastActivePath = game?.FullPath;`. Null-conditional prevents a
  `NullReferenceException` when both the selected game and the current-app fallback
  are null (game unavailable/removed).

### Updates / Additions / Removals
- None (ChangeLog entry only).

## Backward compatibility for 4.17.x
- **Settings.xml / WebService API / data model:** unaffected. One-character
  null-guard, no contract change.
- Restored tree verified: `App.v4/Global.cs:90` still has the unguarded
  `game.FullPath` — fix is missing.

## Decision
**Apply.** Path translation: `x360ce.App.Beta/Global.cs` → `App.v4/Global.cs:90`.
Skip the ChangeLog hunk. Lowest-possible-risk crash fix.

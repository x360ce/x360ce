using System;

namespace x360ce.App
{
    public enum TaskName: int
    {
        None = 0,
		SearchSettings,
		DeleteSettings,
		SearchSummaries,
		SearchPresets,
		CloudCommand,
        GetPrograms,
    }
}
